name: changelog
on:
  workflow_dispatch:

jobs:
  changelog:
    runs-on: ubuntu-latest
    steps:
      - name: 🤘 checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: master
          token: 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

      - name: ⚙ changelog
        uses: faberNovel/github-changelog-generator-action@master
        with:
          options: --token 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 --o changelog.md

      - name: 🚀 changelog
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add changelog.md
          git commit -m "🖉 Update changelog with ${GITHUB_REF#refs/*/}"
          git push -f origin master
